const PlayButton = () => {
  return (
    <button className="cta-btn bg-accent hover:bg-light text-xl text-light hover:text-accent font-bold">
      Play Now
    </button>
  );
};

export default PlayButton;
